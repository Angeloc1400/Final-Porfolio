#Angel Ocampo
#November 8, 2022
#Program will take the number from the user and calculate the factorial of that number in 2 ways.
#It will use a for loop and using the math module

#best attempt of using for loop
import math
num = int(input("Enter your number:"))


def cal_factorial(num):
    factorial=1
    if num == 0 or num == 1:
        return 1
    for i in range(1, num+1):
        factorial = factorial * i
    return factorial
output= cal_factorial(num)
print("the factoral number of",num ,"is", output)

#Option 2
#using the math import to find the factoral of a number
import math

nums = int(input("Enter your number:"))

Answer=math.factorial(nums)
print("the factoral number of",nums ,"is", Answer)

